# fly.toml app configuration file generated for glarion-api on 2026-08-29T14:10:13+02:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'glarion-api'
primary_region = 'cdg'

[build]
[env]
  PORT = '8080'
  RUST_LOG = 'info,sqlx=warn'
  # [http_service] means fly-proxy terminates every connection and sets
  # Fly-Client-IP itself, so it is the only address that identifies a caller.
  # Without this the rate limiters bucket the entire internet together.
  TRUST_PROXY_CLIENT_IP = '1'

[processes]
  app = '/usr/local/bin/api'
  worker = '/usr/local/bin/runner'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    interval = '15s'
    timeout = '5s'
    grace_period = '10s'
    method = 'GET'
    path = '/health'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'
  processes = ['app']

[[vm]]
  size = 'shared-cpu-1x'
  memory = '1gb'
  processes = ['worker']
